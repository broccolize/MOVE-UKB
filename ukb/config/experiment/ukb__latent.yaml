# @package _global_

defaults:
  - data: ukb_proteomics
  - task: latent_space

task:
  batch_size: 64

  model:
    num_hidden: [1024, 512]
    num_latent: 32
    dropout: 0.1

  training_loop:
    lr: 1e-3
    num_epochs: 50
    early_stopping: true
